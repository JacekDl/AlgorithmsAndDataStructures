package algorithms.sortingalgorithms.bucketsort;

import datastructures.lists.LinkedList;

public class BucketSort {
	
	public void sortByBucketing(double[] aArray) {
		LinkedList[] bArray = new LinkedList[aArray.length];
		
		/*for(int i = 0; i < aArray.length; i++) {
			bArray.add(aArray[i]);
		}*/
		
		
		//double[] bArray = new double[aArray.length];
		
		//LinkedList<double[]> bArray = new LinkedList<>();
		
		/*List<Integer> bArray = new List<>();
		for(int i = 0; i < bArray.length; i++) {
			bArray[i] = new LinkedList<Integer>();
			
		}*/
		
		
		/*double[] bArray = new double[aArray.length];
		for(int i = 0; i < bArray.length; i++) {
			LinkedList bArray = new LinkedList();
			
		}*/
	}

}
